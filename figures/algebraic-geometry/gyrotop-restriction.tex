% The diagram of restriction maps for the punctured gyrotop: the global
% section ring A = O_X(X) restricts to O_X(U), which restricts to both
% O_X(D(z)) and O_X(V), each restricting to O_X(empty) = 0.
% Source: book/tex/alg-geom/spec-sheaf.tex (§ problem "Punctured gyrotop") — tikzcd.
\documentclass[border=2mm]{standalone}
\usepackage{tikz}
\usepackage{tikz-cd}
\usepackage{amssymb}
\providecommand{\OO}{\mathcal{O}}
\begin{document}
\begin{tikzcd}
	& \OO_X(X) = A \ar[d] \ar[ldd] \ar[rdd] & \\
	& \OO_X(U) \ar[ld] \ar[rd] & \\
	\OO_X(D(z)) \ar[rd] & & \OO_X(V) \ar[ld] \\
	& \OO_X(\varnothing) = 0 &
\end{tikzcd}
\end{document}
